//- extends layout.pug
//- include utils.pug
//- block content
//-     +validationErrorSummary(errors)
//-     form(action=`/stories/${story.id}/edit` method='POST')
//-         input(name='_csrf' type='hidden' value=csrfToken)
//-         label Title
//-         input(type='text' name='title' required value=story.title)
//-         label Content
//-         input(type='text' name='content' required value=story.content)
//-         label Image
//-         input(type='text' name='imgUrl' required value=story.imgUrl)
//-         button(type="submit") Delete
//-         a(href=`/stories/${story.id}`) Cancel
extends layout.pug

block content
  h3= story.title
    p= story.content
  div(class='py-4')
    p Proceed with deleting this story?
  div
    form(action=`/stories/${story.id}/delete` method='post')
      input(type='hidden' name='_csrf' value=csrfToken)
      button(type='submit') Delete Story
      a(href=`/stories/${story.id}` role='button') Cancel
